# AgriFlow environment variables (example — copy to .env and fill in).
# .env is gitignored; this file contains no secrets and is safe to commit.

# Required only for the LLM assistant (/assistant/query, /assistant/insights).
# The app still boots and serves every other endpoint without it.
OPENAI_API_KEY=
